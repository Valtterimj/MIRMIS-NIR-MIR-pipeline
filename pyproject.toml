[project]
name = "mirmis-nir-mir-pipeline"
version = "0.1.0"
description = "Ground processing pipeline for MIRMIS/NIR-MIR instrument"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
